
예제를 보지 않고 처음부터 끝까지 구현. Functional 프로그래밍에 대한 개념과 아래 코드에서 사용된 메소드에 대해 전반적으로 이해함.

### Property Observers

- 스위프트는 항상 struct구조의 변경을 감지함
- 구조가 변경될 때 특정 동작을 실행하게 할 수 있음

```jsx
var isSelected: Bool {
		willSet {
			if newValue {
							someBusinessLogic()
			} else {
					    someOtherBusinessLogic()
				}	
		}
}
```

ReactNative에서 useEffect로 특정 변수를 ‘subscribe’하는 원리와 유사하다고 생각됨

위 예시 코드에 ‘willSet’ 프로퍼티 뿐만 아니라 ‘didset’ 프로퍼티를 사용하여 변경이 동작이 감지되고 완료된후 특정 로직을 실행시킬 수 있음 (react componentdidmount와 유사)

### LazyHStack and LazyVstack, What is the lazy thing...

`lazy` 변수는 처음 사용되기 전까지는 연산이 되지 않는다. 반면 computed property는 특정한 값을 연산하여 반환한다.

이렇게 lazy는 화면 인터페이스 보여주지만 사용하기도 전에 불러오기 부담스러운 것들은  lazy를 사용한다.

이처럼 필요하지만 사용하기도 전에 불러오기  것들은 `lazy`를 통해 선언해줄 수 있다

### 

## Clean code Rules

### 1. syntax가 길어질 경우 해당 속성을 리턴하는 Function을 사용

```jsx
struct CardView: View {
    let card: EmojiMemoryGame.Card
    var body: some View {
						 ....       
            GeometryReader(content: {geometry in
                ZStack {
                   ....
                        Text(card.content).font(font(in: geometry.size))
                ....
                }
            })
        }
    }
    
    private func font(in size:CGSize) -> Font {
        Font.system(size: min(size.width, size.height * CardStyleConstants.fontSacle))
    }
  
}
```

### 2. nested struct을 사용하여 상수값 선언

```jsx
struct CardView: View {
    let card: EmojiMemoryGame.Card
    var body: some View {
        let shape = RoundedRectangle(cornerRadius: CardStyleConstants.cornerRadius)
       
    ......
		    
    private struct CardStyleConstants {
        static let cornerRadius: CGFloat = 20
        static let lineWidth: CGFloat = 3
        static let fontSacle: CGFloat = 0.3
    }
}
```


## MODEL
```swift
// MARK: MODEL

import Foundation

struct MemoryGame<CardContent> where CardContent: Equatable {
    private(set) var cards: [Card]
    
    // Shorten Code With Functional Programming
    private var prevIndex: Int? {
        get {return cards.indices.filter({ cards[$0].isSelected}).onAndOnly}
        set {cards.indices.forEach { cards[$0].isSelected = ($0 == newValue)}}
    }
    
    mutating func chooseCard (_ card: Card) {
        if let selectedIndex = cards.firstIndex(where: {$0.id == card.id}),
           !cards[selectedIndex].isMatched,
           !cards[selectedIndex].isSelected {
            if let matchedIndex = prevIndex {
                print(matchedIndex)
                if cards[selectedIndex].content == cards[matchedIndex].content {
                    /* set ture value */
                    cards[selectedIndex].isMatched = true
                    cards[matchedIndex].isMatched = true
                }
                cards[selectedIndex].isSelected.toggle()
            }
            else {
                print("AIM2")
                prevIndex = selectedIndex
            }
        }
    }
    
    // Initialize card list
    init(pairOfIndexOfCard: Int, createContent: (Int) -> CardContent) {
        // initialize cards in init statement
        cards = []
        
        // Add two Pairof Card
        for index in 0 ..< pairOfIndexOfCard {
            let content: CardContent = createContent(index)
            cards.append(Card(content: content, id: index * 2))
            cards.append(Card(content: content, id: index * 2 + 1))
        }
    }
    
    struct Card: Identifiable {
        var isSelected: Bool = false
        var isMatched: Bool = false
        var content: CardContent
        var id: Int
    }
}

extension Array {
    var onAndOnly: Element? {
        if self.count == 1 {
            return first
        } else {
            return nil
        }
    
}
```

## VIEW-MODEL

```swift
// MARK: VIEW MODEL

import SwiftUI

class EmojisMemoryGame: ObservableObject {
    private static let emojis = ["😈", "😎", "💫", "👻", "👹", "🙉", "❤️", "💩", "👺", "💛", "💕", "😬", "😏", "😊"]
    @Published private var model: MemoryGame<String> = MemoryGame<String>(pairOfIndexOfCard: 4) { index in
        emojis[index]
    }
    // Expose Needed model Data
    var cards: [MemoryGame<String>.Card] {
        return model.cards
    }
    
    // MARK: INTENT - choose card Logic
    func chooseCard(_ card: MemoryGame<String>.Card) {
        model.chooseCard(card)
    }
}
```

## VIEW

```swift
import SwiftUI

let emojis = ["😈", "😎", "💫", "👻", "👹", "🙉", "❤️", "💩", "👺", "💛", "💕", "😬", "😏", "😊"]

struct EmojisMemoryGameView: View {
    @ObservedObject var game: EmojiMemoryGame
    var body: some View {
        VStack {
            LazyVGrid(columns: [GridItem(.adaptive(minimum: 100))]) {
                ForEach(game.cards) { card in
                    CardView(card: card)
                        .aspectRatio(2/3, contentMode: .fit)
                        .onTapGesture {
                            game.chooseCard(card: card)
                        }
                }
            }
            Spacer()
        }
        .padding()
    }
}

struct CardView: View {
    let card: EmojiMemoryGame.Card
    var body: some View {
        let shape = RoundedRectangle(cornerRadius: CardStyleConstants.cornerRadius)
        VStack {
            GeometryReader(content: {geometry in
                ZStack {
                    if card.isSelected {
                        shape.foregroundColor(Color.white)
                        shape.strokeBorder(lineWidth: CardStyleConstants.lineWidth).foregroundColor(Color.red)
                        Text(card.content).font(font(in: geometry.size))
                    } else if card.isMatched {
                        shape.opacity(0.0)
                    }
                    else {
                        shape.foregroundColor(Color.red)
                    }
                }
            })
        }
    }
    
    private func font(in size:CGSize) -> Font {
        Font.system(size: min(size.width, size.height * CardStyleConstants.fontSacle))
    }
    
    private struct CardStyleConstants {
        static let cornerRadius: CGFloat = 20
        static let lineWidth: CGFloat = 3
        static let fontSacle: CGFloat = 0.3
    }
}

struct ContentView_Previews: PreviewProvider {
    static var previews: some View {
        EmojisMemoryGameView(game: EmojiMemoryGame())
    }
}
}
```
